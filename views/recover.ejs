<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Recovery | TechSymposium '26</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(45deg, #0f0c29, #302b63, #24243e); }
        .recover-card { width: 100%; max-width: 440px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); border-radius: 16px; backdrop-filter: blur(10px); }
        .glass-input { background: rgba(255,255,255,0.12) !important; border: 1px solid rgba(255,255,255,0.2); color: #fff !important; }
        .glass-input:focus { background: rgba(255,255,255,0.18) !important; border-color: #00c6ff; box-shadow: none; }
        .form-label { color: rgba(255,255,255,0.7); }
    </style>
</head>
<body>
    <div class="recover-card p-4 p-md-5 text-white">
        <h2 class="fw-bold mb-2">Recover Account</h2>
        <p class="text-white-50 mb-4">Enter your email to receive a secure recovery code.</p>

        <% if (locals.error && error) { %>
            <div class="alert alert-danger py-2"><%= error %></div>
        <% } %>
        <% if (locals.success && success) { %>
            <div class="alert alert-success py-2"><%= success %></div>
        <% } %>

        <form action="/recover/request" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-3">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" id="email" name="email" class="form-control glass-input" required>
            </div>
            <button type="submit" class="btn btn-warning w-100 fw-bold">Send Recovery Code</button>
            <a href="/" class="btn btn-outline-light w-100 mt-2">Back to Login</a>
        </form>
    </div>
</body>
</html>
