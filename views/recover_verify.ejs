<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Recovery | TechSymposium '26</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(45deg, #0f0c29, #302b63, #24243e); }
        .recover-card { width: 100%; max-width: 460px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); border-radius: 16px; backdrop-filter: blur(10px); }
        .glass-input { background: rgba(255,255,255,0.12) !important; border: 1px solid rgba(255,255,255,0.2); color: #fff !important; }
        .glass-input:focus { background: rgba(255,255,255,0.18) !important; border-color: #00c6ff; box-shadow: none; }
        .form-label { color: rgba(255,255,255,0.7); }
    </style>
</head>
<body>
    <div class="recover-card p-4 p-md-5 text-white">
        <h2 class="fw-bold mb-2">Verify Recovery</h2>
        <p class="text-white-50 mb-4">Enter the code from your email and set a new phone number.</p>

        <% if (locals.error && error) { %>
            <div class="alert alert-danger py-2"><%= error %></div>
        <% } %>

        <form action="/recover/verify" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-3">
                <label for="code" class="form-label">Recovery Code</label>
                <input type="text" id="code" name="code" class="form-control glass-input" maxlength="6" pattern="\d{6}" required>
            </div>
            <div class="mb-3">
                <label for="new_phone" class="form-label">New Phone Number</label>
                <input type="tel" id="new_phone" name="new_phone" class="form-control glass-input" maxlength="10" pattern="\d{10}" required>
            </div>
            <button type="submit" class="btn btn-success w-100 fw-bold">Verify and Update</button>
            <a href="/recover" class="btn btn-outline-light w-100 mt-2">Back</a>
        </form>
    </div>
</body>
</html>
