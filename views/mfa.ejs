<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MFA Verification | TechSymposium '26</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body { min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(45deg, #0f0c29, #302b63, #24243e); }
        .mfa-card { width: 100%; max-width: 420px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); border-radius: 16px; backdrop-filter: blur(10px); }
        .glass-input { background: rgba(255,255,255,0.12) !important; border: 1px solid rgba(255,255,255,0.2); color: #fff !important; }
        .glass-input:focus { background: rgba(255,255,255,0.18) !important; border-color: #00c6ff; box-shadow: none; }
    </style>
</head>
<body>
    <div class="mfa-card p-4 p-md-5 text-white">
        <h2 class="fw-bold mb-2">MFA Verification</h2>
        <p class="text-white-50 mb-4">Enter the 6-digit OTP sent to your email.</p>

        <% if (locals.error && error) { %>
            <div class="alert alert-danger py-2"><%= error %></div>
        <% } %>

        <form action="/mfa" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-3">
                <label for="otp" class="form-label text-white-50">OTP Code</label>
                <input type="text" id="otp" name="otp" class="form-control glass-input" maxlength="6" pattern="\d{6}" required>
            </div>
            <button type="submit" class="btn btn-info w-100 fw-bold">Verify and Continue</button>
            <a href="/" class="btn btn-outline-light w-100 mt-2">Back to Login</a>
        </form>
    </div>
</body>
</html>
