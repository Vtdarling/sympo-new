<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | TECQUORA 2k26</title>
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/vendor/fontawesome/css/all.min.css">
    <link rel="stylesheet" href="/vendor/animate/animate.min.css"/>
    <link rel="stylesheet" href="/css/style.css">
    <style nonce="<%= cspNonce %>">
        body { margin: 0; padding: 0; min-height: 100vh; display: flex; align-items: center; justify-content: center; overflow: hidden; font-family: 'Segoe UI', sans-serif; background: transparent !important; }
        .video-bg { position: fixed; top: 0; left: 0; width: 100%; height: 100%; overflow: hidden; z-index: -1; }
        .video-bg video { min-width: 100%; min-height: 100%; width: auto; height: auto; position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); object-fit: cover; }
        .video-overlay { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.6); z-index: 0; }
        .login-card { background: rgba(16, 16, 16, 0.6); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.15); border-radius: 20px; box-shadow: 0 25px 45px rgba(0,0,0,0.3); width: 100%; max-width: 420px; z-index: 2; position: relative; }
        .glass-input { background: rgba(255, 255, 255, 0.1) !important; border: 1px solid rgba(255, 255, 255, 0.2); color: white !important; }
        .glass-input:focus { background: rgba(255, 255, 255, 0.15) !important; border-color: #00c6ff; }
        .form-floating > .glass-input:not(:placeholder-shown) { background: rgba(255, 255, 255, 0.1) !important; }
        .form-floating > .glass-input:-webkit-autofill,
        .form-floating > .glass-input:-webkit-autofill:hover,
        .form-floating > .glass-input:-webkit-autofill:focus {
            -webkit-text-fill-color: #fff !important;
            -webkit-box-shadow: 0 0 0px 1000px rgba(255, 255, 255, 0.1) inset !important;
            box-shadow: 0 0 0px 1000px rgba(255, 255, 255, 0.1) inset !important;
            transition: background-color 9999s ease-in-out 0s;
        }
        .btn-glow { background: linear-gradient(45deg, #00c6ff, #0072ff); border: none; color: white; padding: 12px; font-weight: 700; transition: all 0.4s ease; }
        .btn-glow:hover { box-shadow: 0 0 15px #00c6ff; transform: scale(1.05); }
        .btn-outline-glow { border: 2px solid rgba(255, 255, 255, 0.3); color: white; padding: 10px; font-weight: 600; text-decoration: none; display: block; text-align: center; }
        .btn-outline-glow:hover { border-color: #fff; background: rgba(255, 255, 255, 0.1); color: white; }
        .content-container { position: relative; z-index: 1; min-height: 100vh; }
        .form-floating label { color: rgba(255, 255, 255, 0.65) !important; }
        .form-floating > .form-control:focus ~ label,
        .form-floating > .form-control:not(:placeholder-shown) ~ label {
            color: #ffffff !important;
        }
        #client-error-msg { display: none; background: rgba(220,53,69,0.2); border: 1px solid #dc3545; color: #ffcccc; padding: 10px; border-radius: 8px; margin-bottom: 20px; }
        #server-error-msg { background: rgba(220,53,69,0.2); border: 1px solid #dc3545; color: #ffcccc; padding: 10px; border-radius: 8px; margin-bottom: 20px; }
        #server-success-msg { background: rgba(25,135,84,0.25); border: 1px solid #198754; color: #d1f7e3; padding: 10px; border-radius: 8px; margin-bottom: 20px; }
        .visible-block { display: block !important; }
    </style>
</head>
<body>
    <div class="video-bg">
        <video autoplay muted loop playsinline poster="/images/bg-fallback.jpg">
            <source src="/images/login_bg.mp4" type="video/mp4">
        </video>
        <div class="video-overlay"></div>
    </div>

    <div class="container d-flex justify-content-center align-items-center content-container">
        <div class="login-card p-4 p-md-5 animate__animated animate__zoomIn">
            <div class="text-center mb-4">
                <i class="fas fa-user-astronaut fa-3x text-info mb-3"></i>
                <h2 class="fw-bold text-white mb-1">Participants Login</h2>
                <p class="text-white-50 small">Enter your credentials</p>
            </div>

            <% if (locals.error && error !== null) { %>
                <div id="server-error-msg"><i class="fas fa-exclamation-triangle me-2"></i> <%= error %></div>
            <% } %>
            <% if (locals.success && success !== null) { %>
                <div id="server-success-msg"><i class="fas fa-check-circle me-2"></i> <%= success %></div>
            <% } %>
            <div id="client-error-msg"></div>

            <form action="/login" method="POST" id="loginForm">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="form-floating mb-3">
                    <input type="email" name="email" class="form-control glass-input" id="floatingEmail" placeholder="name@example.com" required>
                    <label for="floatingEmail">Email Address</label>
                </div>
                <div class="form-floating mb-4">
                    <input type="tel" name="phone" class="form-control glass-input" id="floatingPhone" placeholder="Phone" maxlength="10" required>
                    <label for="floatingPhone">Phone Number</label>
                </div>
                
                <div class="d-grid gap-3">
                    <button type="submit" class="btn btn-glow rounded-pill btn-lg">Get In</button>
                    <!-- UPDATED LINK TO NEW SIGNUP PAGE -->
                    <a href="/signup" class="btn btn-outline-glow rounded-pill">Register New Account</a>
                </div>
            </form>
        </div>
    </div>

    <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script nonce="<%= cspNonce %>">
        window.addEventListener('load', function() {
            localStorage.removeItem('sympo_event_id');
        });
        function validateForm() {
            const email = document.getElementById('floatingEmail').value;
            const phone = document.getElementById('floatingPhone').value;
            const err = document.getElementById('client-error-msg');
            if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { err.innerText = "Invalid Email"; err.classList.add('visible-block'); return false; }
            if (!/^\d{10}$/.test(phone)) { err.innerText = "Phone must be 10 digits"; err.classList.add('visible-block'); return false; }
            err.classList.remove('visible-block');
            return true;
        }

        document.getElementById('loginForm').addEventListener('submit', function(event) {
            if (!validateForm()) {
                event.preventDefault();
            }
        });
    </script>
</body>
</html>